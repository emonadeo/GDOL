{
	"name": "gdol",
	"version": "0.1.0",
	"description": "",
	"scripts": {
		"atlas:clean": "deno task atlas:clean",
		"atlas:apply": "deno task atlas:apply",
		"build": "deno task build",
		"build:api": "deno task build:api",
		"build:web": "deno task build:web",
		"dev": "deno dev",
		"dev:api": "deno task dev:api",
		"dev:web": "deno task dev:web",
		"fmt": "prettier --write .",
		"lint:web": "deno task lint:web",
		"seed": "deno task seed"
	},
	"license": "MIT",
	"devDependencies": {
		"prettier": "^2.8.7",
		"prettier-plugin-astro": "^0.8.0",
		"prettier-plugin-sql-cst": "^0.8.2"
	}
}
